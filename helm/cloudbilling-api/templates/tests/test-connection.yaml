apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudbilling-api.fullname" . }}-test-connection"
  labels:
    {{- include "cloudbilling-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudbilling-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
